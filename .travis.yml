language: java
jdk:
  - oraclejdk8 
cache:
  directories:
    - $HOME/.m2
install: ./mvnw -pl 'vertx-vaadin' install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script: ./mvnw -pl 'vertx-vaadin' clean verify
branches:
  only:
    - master
